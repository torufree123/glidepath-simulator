# グライドパス・シミュレーションツール 仕様書

| 項目 | 内容 |
|---|---|
| 文書名 | グライドパス・シミュレーションツール 仕様書 |
| バージョン | v0.1(ドラフト) |
| 作成日 | 2026-08-08 |
| 想定読者 | プロダクト企画 / 開発チーム / コンプライアンス |
| ステータス | 社内レビュー待ち |
| 関連システム | MILIZE AM、milize-mcp-server、FA-OS(ガードレール体系) |

---

## 1. 背景と目的

### 1.1 背景

グライドパス(Glide Path)は、ターゲットイヤーファンド(TYF/TDF)の中核概念であり、年齢(またはターゲット年までの残存期間)に応じてリスク資産比率を逓減させる資産配分の経路を指す。確定拠出年金(企業型DC・iDeCo)における指定運用方法(デフォルト商品)としてターゲットイヤー型の採用が広がっており、以下の三つの場面で定量評価ニーズがある。

1. **商品企画**: 運用会社・信託・銀行がグライドパス形状を設計・改定する際の確率分布ベースの比較評価
2. **制度設計**: DC運営管理機関・事業主がデフォルト商品を選定する際の中立的な比較情報
3. **加入者向け説明**: B2B2E(職域)アプリやFP面談における、個人条件を反映した将来資産分布の可視化

### 1.2 目的

本ツールは、パラメトリックに定義したグライドパスに対しモンテカルロ・シミュレーションを実行し、資産残高分布・目標達成確率・資産枯渇確率等を算出する**決定論的計算エンジン**と、その出力のみを根拠に平易な説明文を生成する**LLM説明層**から構成される。目的は次の4点。

1. グライドパス設計(To型/Through型、逓減カーブ)の定量評価
2. 複数設計・既存商品・固定配分ベンチマークとの同一条件比較
3. 個人条件(年齢・拠出・目標額・取り崩し計画)での適合性シミュレーション
4. 前提条件と免責を明示した顧客向け説明資料の自動生成

### 1.3 スコープ外(本ツールがやらないこと)

- 特定金融商品の推奨、売買の勧誘、投資助言に該当する出力
- 実売買・発注・口座連携
- 個別ファンドの将来リターン「予測」(あくまで前提に基づくシナリオ計算)
- 公的年金の精密な受給額計算(簡易入力値としてのみ受け付ける)

---

## 2. 用語定義

| 用語 | 定義 |
|---|---|
| グライドパス | 年齢または残存期間に応じたリスク資産比率 w(t) の経路 |
| ターゲットイヤー | 退職・受給開始等の目標年。グライドパスの着地基準点 |
| To型 | ターゲット年で最終配分に到達し、以後固定する設計 |
| Through型 | ターゲット年以降も逓減を継続し、取り崩し期を通じて配分が変化する設計 |
| リスク資産比率 | 株式等の価格変動の大きい資産クラスの合計ウェイト |
| CMA | Capital Market Assumptions。資産クラス別の期待リターン・ボラティリティ・相関の前提セット |
| 成功確率 | 評価時点の資産額が目標額以上となるパスの比率 P(V_T ≥ G) |
| ショートフォール | 目標未達額 max(G − V_T, 0)。期待値を期待ショートフォールと呼ぶ |
| 資産枯渇(ルイン)確率 | 取り崩し期に資産が0以下となるパスの比率(年齢別に算出) |
| 収益率配列リスク(SoRR) | 同一平均リターンでも下落の発生順序により結果が変わるリスク。特に退職直前・直後の下落 |
| CVaR(5%) | 下位5%パスの平均値(条件付きバリュー・アット・リスク) |
| ファンチャート | 残高分布のパーセンタイル帯を時系列表示するグラフ |
| CRN | Common Random Numbers。比較対象間で同一乱数系列を用い、差分の分散を抑える技法 |

---

## 3. アクターとユースケース

### 3.1 アクター

| ID | アクター | 主な利用目的 |
|---|---|---|
| A1 | 商品企画担当(運用会社等) | グライドパス形状の設計・改定評価 |
| A2 | DC運営管理機関・事業主 | デフォルト商品候補の中立比較 |
| A3 | FP・営業職員 | 面談での個人向けシミュレーション提示 |
| A4 | 加入者・個人(B2B2E) | 自分の条件での将来分布の確認 |

### 3.2 ユースケース

| ID | ユースケース | 概要 |
|---|---|---|
| UC-01 | 設計比較 | 複数のグライドパス定義を同一乱数系列で並列比較 |
| UC-02 | 個人適合シミュレーション | 年齢・拠出・目標を入力し達成確率と分布を提示 |
| UC-03 | 既存商品ベンチマーク | 目論見書等から取り込んだ既存TYFのグライドパスと自社設計を比較 |
| UC-04 | ストレステスト | 退職直前ショック等の決定論的シナリオを重ねて頑健性を評価 |
| UC-05 | 取り崩し設計評価 | Through型における資産枯渇確率・資産寿命の分布を評価 |
| UC-06 | 固定配分対比 | バランス型固定配分(例: 株式50%固定)との優劣を同一条件で検証 |

---

## 4. 全体アーキテクチャ

```
[UI / レポート出力]
      │
[API層] REST + MCPツール (simulate_glidepath / compare_glidepaths)
      │
[計算層] 決定論的モンテカルロ・エンジン(Python/NumPy、シード固定)
      │
[結果ストア] SimulationRun(append-only、結果ハッシュ・パラメータ・シード保存)
      │
[LLM説明層] エンジン出力JSONのみを根拠に文章生成(数値の生成・改変禁止)
      │
[ガードレール層] 表現制御(断定的判断・推奨表現の遮断)、免責文の自動付与
```

**設計原則(MILIZE五原則との対応)**

1. **数値はエンジン、言葉はLLM**: すべての数値は計算層が生成し、LLMはテンプレート差し込みされた数値を含む説明文のみを生成する。LLMが数値を計算・加工・丸め直しすることを禁止する。
2. **再現性**: (engine_version, cma_version, params, seed) の組で結果が完全再現できること。
3. **前提の明示**: CMA出所・手数料・税の扱い・インフレ前提を出力物に常時表示する。
4. **ガードレール第一級**: 表現制御はFA-OSのガードレールID体系(G0〜G4)に準拠し、リリース条件に含める。
5. **監査可能性**: 実行ログはappend-onlyで保存し、事後検証可能とする。

---

## 5. 機能要件

### 5.1 グライドパス定義(GP)

| ID | 要件 |
|---|---|
| FR-GP-01 | 区分線形のパラメトリック定義をサポートする: 開始比率 w_start、最終比率 w_end、逓減開始年齢 a_begin、逓減終了年齢 a_end(ターゲット年基準の相対年数指定も可) |
| FR-GP-02 | カーブ形状として linear / step(5年刻み等) / exponential / logistic / table(年齢×比率の直接指定)を選択できる |
| FR-GP-03 | To型/Through型を選択できる。Through型は着地後の第2逓減区間(a_end2, w_final)を定義できる |
| FR-GP-04 | リスク資産比率から資産クラス配分への写像を定義できる。標準資産クラスは6区分(国内株式/先進国株式/新興国株式/国内債券/外国債券/短期資産)とし、区分数・名称は設定で変更可能。リスク資産内・安全資産内の内訳比率は固定分割(デフォルト)または年齢依存分割を選択できる |
| FR-GP-05 | 既存商品のグライドパスをCSV(age または years_to_target, 資産クラス別ウェイト)でインポートできる |
| FR-GP-06 | 制約検証: 各時点で Σw_i = 1、0 ≤ w_i ≤ 1。リスク資産比率の非単調(再上昇)を検出した場合は警告を表示する(エラーとはしない) |

### 5.2 資本市場前提(CMA)

| ID | 要件 |
|---|---|
| FR-CMA-01 | 資産クラス別に期待リターン(算術/幾何の別を必ず明示)、年率ボラティリティ、相関行列を保持する |
| FR-CMA-02 | 相関行列の正定値性を検証する。コレスキー分解に失敗した場合は最近接正定値行列へ補正し、補正実施を警告として記録する |
| FR-CMA-03 | CMAセットをバージョン管理する(id、version、出所、作成日)。標準/保守/楽観の複数セットを登録・切替できる |
| FR-CMA-04 | 年率インフレ率を保持する(v0.1は確定値。確率過程化はPhase 2) |

### 5.3 キャッシュフロー設定

| ID | 要件 |
|---|---|
| FR-CF-01 | 積立: 月額拠出、年次改定率(昇給連動)、賞与月加算、拠出開始/終了年齢を設定できる |
| FR-CF-02 | 拠出上限はテンプレート(企業型DC/iDeCo等)を設定マスタで管理し、法改正に追随して更新できる。上限超過入力は警告する(仕様書に法定値をハードコードしない) |
| FR-CF-03 | 取り崩し: 定額(名目固定/実質固定)、定率(残高×x%)、期間指定の3方式と開始年齢を設定できる |
| FR-CF-04 | 一時金・スポット入出金イベント(退職一時金の追加投入、住宅資金の引き出し等)を任意時点に設定できる |
| FR-CF-05 | (オプション)死亡率テーブルを設定し、生存率加重した指標(生存加重枯渇確率等)を併記できる(Phase 2) |

### 5.4 シミュレーションエンジン

| ID | 要件 |
|---|---|
| FR-SIM-01 | 多変量対数正規(コレスキー分解による相関付与)の月次モンテカルロを実行する。パス数はデフォルト10,000(設定範囲 1,000〜100,000) |
| FR-SIM-02 | 乱数生成器はPCG64。シードは実行パラメータとして保存し、完全再現を保証する |
| FR-SIM-03 | リバランス方式: 毎月グライドパス目標配分に一致(デフォルト。TYFの実挙動近似)/ 乖離バンド方式(±x%超過時のみ) |
| FR-SIM-04 | コスト: 信託報酬(年率)を月次換算で控除、固定費(口座管理料/月)を控除できる |
| FR-SIM-05 | 税制モード: v0.1は「非課税(DC/NISA想定)」と「受取時に評価益へ一律20.315%課税する簡易近似」の2モード。特定口座のロット単位課税はPhase 2 |
| FR-SIM-06 | 名目値に加え、インフレ率でデフレートした実質値を併記できる |
| FR-SIM-07 | (Phase 2)ヒストリカル・ブートストラップ、レジームスイッチング・モデルを選択できる |

### 5.5 出力指標

| ID | 要件 |
|---|---|
| FR-OUT-01 | 残高のパーセンタイル系列(5/25/50/75/95)を月次で出力する(ファンチャート用) |
| FR-OUT-02 | 目標達成確率 P(V_T ≥ G)、ショートフォール確率、期待ショートフォール E[max(G − V_T, 0)] を出力する |
| FR-OUT-03 | 最終資産の分布統計(平均、中央値、CVaR 5%)を出力する |
| FR-OUT-04 | 経路指標: パス別最大ドローダウンの分布、ターゲット年直前5年間の下落幅分布を出力する |
| FR-OUT-05 | 取り崩し期: 年齢別の資産枯渇確率(例: 80/85/90/95歳時点)、資産寿命(枯渇年齢)の分布を出力する |
| FR-OUT-06 | SoRRストレス: 指定時点(例: ターゲット3年前)に株式クラスへ−X%の決定論的ショックを重ねた場合の指標変化を出力する |
| FR-OUT-07 | ベンチマーク比較: 固定配分・他グライドパスとの比較を同一乱数系列(CRN)で実行し、指標差分を出力する |

### 5.6 比較・感応度

| ID | 要件 |
|---|---|
| FR-CMP-01 | 最大5設定の並列比較を同一シードで実行できる(MILIZE AMの保存ポートフォリオ最大5件と整合) |
| FR-CMP-02 | 主要パラメータ(w_start、w_end、逓減開始年齢、拠出額、信託報酬)の±変化に対する成功確率の感応度を出力する(トルネードチャート用データ) |
| FR-CMP-03 | (Phase 2)グリッドサーチによる準最適化: 目的関数(成功確率最大化、またはCRRA効用最大化)を指定してパラメータ探索できる |

### 5.7 説明生成(LLM層)

| ID | 要件 |
|---|---|
| FR-EXP-01 | LLMへの入力はエンジン出力JSONと固定プロンプトのみとする。数値はテンプレート差し込みとし、LLMによる数値の生成・再計算・丸め直しを禁止する |
| FR-EXP-02 | 表現ガードレール: 断定的判断の提供・特定商品の推奨・元本保証を想起させる表現を、禁止辞書と分類器の二段で遮断する。遮断時は定型文へフォールバックする |
| FR-EXP-03 | 読者レベル(加入者向け/専門家向け)を切り替えられる |

### 5.8 レポート出力

| ID | 要件 |
|---|---|
| FR-REP-01 | PDF/PPTX形式で出力できる。前提条件(CMA出所・手数料・税・インフレ)と免責文を自動掲載する |
| FR-REP-02 | 脚注に engine_version / cma_version / seed / run_id を記載し、第三者が再現可能であることを担保する |

---

## 6. 計算仕様

### 6.1 タイムライン

- 月次ステップ t = 0, 1, ..., T。開始年齢 a_0、評価終了年齢 a_max(デフォルト95歳)。
- 年齢 a(t) = a_0 + t/12。
- フェーズ: 積立期(a < 取り崩し開始年齢)、取り崩し期(それ以降)。

### 6.2 グライドパス関数

**区分線形(To型)の一般形:**

```
w_eq(a) = w_start                                        (a ≤ a_begin)
        = w_start + (w_end − w_start)·(a − a_begin)/(a_end − a_begin)
                                                         (a_begin < a < a_end)
        = w_end                                          (a ≥ a_end)
```

**Through型**は第2区間を追加する: a_end 以降 a_end2 まで w_end → w_final へ線形逓減し、以後 w_final で固定。

**資産クラス配分への写像(固定分割の場合):**

```
w_i(t) = w_eq(a(t)) · φ_i^risky      (i ∈ リスク資産)
w_j(t) = (1 − w_eq(a(t))) · φ_j^safe (j ∈ 安全資産)
ただし Σφ^risky = Σφ^safe = 1
```

### 6.3 リターン生成(多変量対数正規)

CMA入力: 資産クラス i の年率**幾何**期待リターン g_i、年率ボラティリティ σ_i、相関行列 P。

```
月次対数リターンベクトル: ℓ_t = m + S · L · z_t
  m_i = ln(1 + g_i) / 12
  S   = diag(σ_i / √12)
  L   = chol(P)          (コレスキー分解)
  z_t ~ N(0, I)          (i.i.d.)
月次単純リターン: r_{i,t} = exp(ℓ_{i,t}) − 1
```

CMAが**算術**期待リターン μ_i で与えられた場合は対数正規整合の変換 g_i = (1 + μ_i)·exp(−σ_i²/2) − 1 を適用する(近似式 g ≈ μ − σ²/2 を許容誤差の範囲で使用してもよいが、どちらを用いたかを記録する)。

### 6.4 資産推移の再帰式

毎月リバランス(目標配分一致)前提では、資産合計 V_t のみを追跡すればよい。

```
ポートフォリオ月次リターン: R_t = Σ_i w_i(t) · r_{i,t}
コスト控除係数:            κ = (1 − f_ann)^(1/12)     (f_ann: 信託報酬年率)

積立期:     V_{t+1} = (V_t + C_t) · (1 + R_t) · κ − F_fix
取り崩し期: V_{t+1} = max(0, V_t − W_t) · (1 + R_t) · κ − F_fix
```

- C_t: 月次拠出額(年次改定率・賞与月加算を反映)
- W_t: 月次取り崩し額(定額名目 / 定額実質 = インフレ連動 / 定率 = V_t × x/12)
- V_t ≤ 0 に到達したパスは以後 0 に固定し、枯渇月 t_ruin を記録する
- バンドリバランス方式を選択した場合のみ資産クラス別残高を個別追跡する

### 6.5 実質化

```
実質値 V_t^real = V_t / (1 + π)^(t/12)     (π: 年率インフレ率)
```

### 6.6 指標定義

| 指標 | 定義 |
|---|---|
| 目標達成確率 | P(V_T ≥ G) = #{path: V_T ≥ G} / N |
| ショートフォール確率 | P(V_T < G) |
| 期待ショートフォール | (1/N) Σ max(G − V_T, 0) |
| CVaR(5%) | 最終資産の下位5%パスの平均値 |
| 最大ドローダウン | パスごとに max_{s≤t} (1 − V_t / V_s) を計算し、その分布 |
| 枯渇確率(年齢x) | P(t_ruin ≤ x歳到達月) |
| 資産寿命 | t_ruin の分布(枯渇しないパスは打ち切り扱いで別記) |

### 6.7 精度検証(解析解との突合)

拠出ゼロ・取り崩しゼロ・手数料ゼロ・単一資産のケースでは、最終資産は解析的に対数正規分布に従う。

```
ln V_T ~ N( ln V_0 + T_years · ln(1 + g),  σ² · T_years )
理論中央値 = V_0 · (1 + g)^T_years
```

**受け入れ基準**: N = 10,000パス・30年のケースで、モンテカルロ中央値と理論中央値の相対誤差 |err| < 0.5%、かつ5/25/75/95パーセンタイルが理論分位に対し相対誤差 < 1.5% であること。CIで自動検証する。

---

## 7. インターフェース仕様

### 7.1 REST API

| メソッド | パス | 概要 |
|---|---|---|
| POST | /v1/glidepath/simulate | 単一グライドパスのシミュレーション実行 |
| POST | /v1/glidepath/compare | 最大5設定のCRN比較実行 |
| GET | /v1/cma/sets | 登録済みCMAセット一覧 |
| GET | /v1/runs/{run_id} | 過去実行の取得(再現用パラメータを含む) |

**リクエスト例(POST /v1/glidepath/simulate):**

```json
{
  "glidepath": {
    "type": "to",
    "curve": "piecewise_linear",
    "risky_start": 0.90,
    "risky_end": 0.30,
    "decline_begin_age": 45,
    "decline_end_age": 60,
    "risky_split": {"dom_eq": 0.35, "dev_eq": 0.50, "em_eq": 0.15},
    "safe_split": {"dom_bond": 0.60, "for_bond": 0.25, "cash": 0.15}
  },
  "person": {"current_age": 30, "target_age": 60, "end_age": 95},
  "cashflow": {
    "initial_balance": 1000000,
    "monthly_contribution": 23000,
    "contribution_growth_annual": 0.01,
    "withdrawal": {"mode": "fixed_real", "monthly_amount": 150000, "start_age": 65}
  },
  "cma_set_id": "std-2026H1",
  "costs": {"expense_ratio_annual": 0.003, "fixed_fee_monthly": 0},
  "tax_mode": "tax_free",
  "inflation_annual": 0.015,
  "engine": {"n_paths": 10000, "seed": 20260808, "rebalance": "monthly_to_target"},
  "goal": {"target_amount_at_target_age": 30000000},
  "stress": {"equity_shock_pct": -0.30, "years_before_target": 3}
}
```

**レスポンス例(抜粋):**

```json
{
  "run_id": "gp-run-01J...",
  "engine_version": "0.1.0",
  "cma_version": "std-2026H1",
  "seed": 20260808,
  "percentile_paths": {
    "ages": [30.0, 30.083, "..."],
    "p05": ["..."], "p25": ["..."], "p50": ["..."], "p75": ["..."], "p95": ["..."]
  },
  "metrics": {
    "success_prob": 0.71,
    "shortfall_prob": 0.29,
    "expected_shortfall": 4200000,
    "terminal_at_target": {"mean": 36500000, "median": 33200000, "cvar05": 14800000},
    "ruin_prob_by_age": {"80": 0.03, "85": 0.08, "90": 0.14, "95": 0.22},
    "max_drawdown": {"p50": -0.18, "p95": -0.34},
    "stress_overlay": {"success_prob": 0.63}
  },
  "assumptions_echo": {"..."},
  "warnings": [],
  "disclaimer_id": "DSC-001"
}
```

### 7.2 MCPツール定義

milize-mcp-server の既存ツール(simulate_life_cashflow, compare_scenarios)と命名・入出力思想を揃える。

| ツール名 | 概要 |
|---|---|
| simulate_glidepath | 7.1のsimulateと同一スキーマの入力を受け、metrics と要約済みパーセンタイル(年次粒度に間引き)を返す |
| compare_glidepaths | 複数定義をCRNで比較し、指標差分テーブルを返す |

LLMエージェントから呼び出される前提のため、レスポンスは (1) 数値はエンジン確定値のみ、(2) 免責IDを必ず含む、(3) トークン節約のためパスデータは年次粒度に集約、の3点を満たす。

### 7.3 主要オブジェクトのスキーマ概要

| オブジェクト | 主要フィールド |
|---|---|
| GlidePathDefinition | type(to/through), curve, risky_start/end, decline_begin/end_age, (through時) decline_end2_age, risky_final, risky_split, safe_split, もしくは table[] |
| CMASet | id, version, source, assets[]{name, geo_return or arith_return, vol}, corr[][], inflation |
| CashflowPlan | initial_balance, monthly_contribution, growth, bonus_months[], withdrawal{mode, amount/rate, start_age}, events[] |
| SimulationRequest | glidepath, person, cashflow, cma_set_id, costs, tax_mode, engine, goal, stress |
| SimulationResult | run_id, versions, seed, percentile_paths, metrics, assumptions_echo, warnings, disclaimer_id |

---

## 8. データモデル

```
CMASet            (cma_id, version, source, created_at, assets_json, corr_json, inflation)
GlidePathDef      (gp_id, name, owner, definition_json, created_at, imported_from)
SimulationRun     (run_id, gp_id, cma_id, engine_version, seed, params_json,
                   result_hash, created_at, created_by)         ※ append-only
ResultSummary     (run_id, metrics_json, percentile_paths_ref)
DisclaimerMaster  (disclaimer_id, text, applicable_context, valid_from)
```

- SimulationRun は追記専用(UPDATE/DELETE禁止)。result_hash は結果JSONのSHA-256とし、回帰テストのゴールデンラン照合に用いる。
- 個人を特定する情報(氏名・口座等)は本ツールのデータモデルに**持たない**。入力は年齢・金額等の属性値のみとし、B2B2E組み込み時の個人紐付けは呼び出し側システムの責務とする。

---

## 9. 非機能要件

| 分類 | 要件 |
|---|---|
| 性能 | 10,000パス × 840ヶ月(70年) × 6資産 の実行を p95 で3秒以内(単一ノード、NumPyベクトル化前提)。100,000パスは非同期ジョブとし60秒以内 |
| 再現性 | (engine_version, cma_version, params, seed) の組で結果が完全一致すること。依存ライブラリはロックファイルで固定 |
| 監査 | 全実行を append-only ログに記録。結果ハッシュにより事後改変を検出可能とする |
| 収束性 | 主要指標(成功確率)のモンテカルロ標準誤差を併記できること(√(p(1−p)/N)) |
| セキュリティ | 個人特定情報を保持しない設計(上記8章)。B2B導入時のFISC安全対策基準・個人情報保護法上の整理は導入先ごとに別紙で実施 |
| 国際化 | v0.1 は JPY・日本語のみ。通貨・言語の抽象化はコード上は確保しておく |
| 可搬性 | エンジンはUIから独立したPythonパッケージとして提供し、CLI単体実行・バッチ実行が可能であること |

---

## 10. コンプライアンス要件

> 本章は設計上の要求事項であり、法令適合の最終判断は法務・コンプライアンスレビューで確定する。

| ID | 要件 |
|---|---|
| CP-01 | **断定的判断の提供の禁止(金商法38条2号)**: 「必ず」「確実に」「保証」等の断定表現、および将来リターンを確定値として述べる表現を、出力・LLM説明の双方で遮断する(禁止辞書+分類器) |
| CP-02 | **投資助言・特定商品推奨の回避**: 本ツールの出力は一般的・客観的なシミュレーション情報の提供に留め、「このファンドを買うべき」等の個別商品推奨文言を生成しない。比較表示は中立(データ出所明示・並び順の恣意排除)とする |
| CP-03 | **DC運営管理機関の行為準則への配慮**: 運営管理機関経由で加入者へ提示する場合、特定商品の推奨と受け取られない画面構成・文言とする(比較情報の提示に限定) |
| CP-04 | **免責・前提の常時表示**: 「本シミュレーションは一定の前提に基づく試算であり、将来の運用成果を示唆・保証するものではありません」の趣旨を、画面・レポート・API応答(disclaimer_id)のすべてに付与する |
| CP-05 | **前提の透明性**: CMAの出所・作成日、手数料・税・インフレの取り扱いを出力物に明記する |
| CP-06 | **ガードレール対応表**: FA-OSのガードレールID体系(G0〜G4)に本ツールの表現制御を対応付け、リリース判定チェックリストに含める(表現系はG2、越権行為系はG3に分類する想定) |

---

## 11. テスト計画

| 種別 | 内容 |
|---|---|
| 単体 | グライドパス関数の境界値(a_begin = a_end、w_start = w_end、table指定の端点)、配分合計=1の検証、拠出上限警告 |
| 統計 | 6.7の解析解突合(CI自動実行)。相関の再現性: 生成リターンの標本相関がCMA相関に収束すること。パス数と標準誤差の関係確認 |
| 回帰 | 固定シードのゴールデンラン結果ハッシュ一致。engine_version更新時は差分理由を記録 |
| 性能 | 9章の性能要件に対する負荷試験 |
| 表現 | ガードレール辞書テスト(禁止表現を含む出力が遮断されフォールバックすること)。LLM説明のファクトチェック: 説明文中の数値がエンジン出力と完全一致することの自動検証 |
| 異常系 | 非正定値相関行列の補正、枯渇パス処理、n_paths上限超過、CMA未登録ID |

---

## 12. 開発フェーズ(目安)

| フェーズ | 期間目安 | 内容 | 完了基準 |
|---|---|---|---|
| Phase 0 | 2週間 | エンジンコア(GP関数・MC・指標)+ CLI + 解析解突合 | 6.7の受け入れ基準をCIでグリーン |
| Phase 1 | 4週間 | REST API・MCPツール化、比較(CRN)、ファンチャートUI、PDF出力、ガードレール層 | UC-01/02/06 がエンドツーエンドで動作 |
| Phase 2 | 別途計画 | 準最適化、ブートストラップ/レジーム、税制精緻化、死亡率加重、確率的インフレ | — |

---

## 13. 未決事項(Open Issues)

| # | 論点 | 備考 |
|---|---|---|
| 1 | 標準CMAの出所 | 社内CMAを新規策定するか、公開情報(例: GPIF基本ポートフォリオ策定時の期待リターン等)を参照ベースにするか |
| 2 | Through型の着地後下限 | w_final の下限値ポリシー(完全安全資産化を許すか) |
| 3 | 税制近似の許容度 | 受取時一括課税近似で商品企画用途に十分か、コンプラ・企画双方の確認 |
| 4 | 既存TYFデータの取得 | 目論見書ベースの手入力運用か、データベンダー契約か |
| 5 | リスクレベル診断との接続 | MILIZE AMの6段階リスクレベルとグライドパス開始比率のマッピング規則 |
| 6 | B2B2E組み込みのUI責務分界 | ファンチャート描画を本ツール側で提供するか、データのみ返すか |

---

## 14. 付録

### A. グライドパス形状の設定例(区分線形・To型)

| 年齢 | 積極型の例 w_eq | 標準型の例 w_eq | 保守型の例 w_eq |
|---|---|---|---|
| 〜40歳 | 0.95 | 0.85 | 0.70 |
| 50歳 | 0.80 | 0.65 | 0.50 |
| 60歳(ターゲット) | 0.45 | 0.35 | 0.25 |
| 60歳以降(To型) | 0.45固定 | 0.35固定 | 0.25固定 |

※ 数値は説明用の例であり、推奨値ではない。

### B. 参考理論・文献(概念整理用)

- ライフサイクル投資と人的資本: Bodie, Merton & Samuelson (1992), "Labor Supply Flexibility and Portfolio Choice in a Life Cycle Model"
- 収益率配列リスク・取り崩し研究: Bengen (1994) の定率取り崩し研究系譜、Blanchett らの動的取り崩し研究
- 制度背景: 確定拠出年金法改正に伴う指定運用方法(デフォルト商品)の整備、米国におけるQDIA(2006年年金保護法)以降のTDF拡大

### C. 変更履歴

| 版 | 日付 | 変更内容 |
|---|---|---|
| v0.1 | 2026-08-08 | 初版ドラフト作成 |
